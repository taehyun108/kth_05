# ── 수집 (키 불요) ─────────────────────────────────────────────────────────
# 1순위 언론사 RSS(pipeline/rss_sources.yaml) · 2순위 구글 뉴스 RSS 모두 키가 없다.
#
# (선택·기본 비활성) 네이버 API HUB 뉴스 검색.
#   구 네이버 검색 API 는 2026-06 HUB 로 이관, 2026-07-31 개발자센터 신규 신청 종료.
#   네이버클라우드 계정 + 결제수단이 필요해 현재 쓰지 않는다.
#   아래 두 값을 채우면 그때 켜진다. 비워 두면 호출조차 하지 않는다.
NAVER_HUB_KEY_ID=
NAVER_HUB_KEY=
